namespace MyShelterWin64.Game.AI {
    /// <summary>
    /// NPC's state machine states' id
    /// </summary>
    public enum AIStateMachine {
        Idle = 0,
        Wander = 1,
        AwareOfDanger = 2,
        Attack = 3,
    }
}